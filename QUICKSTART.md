# RaceTracker Backend - Быстрый старт

## 📦 Структура проекта

```
cicd/
├── backend/                    # Backend приложение FastAPI
│   ├── src/                   # Исходный код
│   │   ├── main.py            # Точка входа
│   │   ├── framework/         # Layer 1: FastAPI endpoints
│   │   ├── business/          # Layer 2: Бизнес-логика
│   │   └── data/              # Layer 3: База данных
│   ├── tests/                 # Unit-тесты
│   ├── requirements.txt       # Зависимости
│   ├── Dockerfile             # Docker конфигурация
│   └── README.md              # Полная документация
├── dev/
│   ├── BACKEND_INSTRUCTIONS.md    # Исходные инструкции
│   └── FRONTEND_INSTRUCTIONS.md   # Инструкции фронтенда
└── IMPLEMENTATION_SUMMARY.md  # Отчет о реализации
```

## 🚀 Быстрый старт (5 минут)

### 1. Установка зависимостей
```bash
cd backend
pip install -r requirements.txt
```

### 2. Запуск сервера
```bash
python -m uvicorn src.main:app --reload --host 0.0.0.0 --port 8000
```

### 3. Проверка здоровья
```bash
curl http://localhost:8000/health
# Ответ: {"status": "healthy"}
```

### 4. Открыть документацию
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

## 📝 Примеры API запросов

### Создание владельца (необходимо для лошади)
```bash
curl -X POST "http://localhost:8000/api/v1/owners" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Иван Петров",
    "address": "Москва, ул. Парковая 5",
    "phone": "+7-900-123-45-67"
  }'
```
Ответ включит `"id": 1`

### Создание жокея
```bash
curl -X POST "http://localhost:8000/api/v1/jockeys" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Алексей Смирнов",
    "address": "Москва, ул. Спортивная 10",
    "age": 25,
    "rating": 8
  }'
```
Ответ включит `"id": 1`

### Создание лошади (требует owner_id)
```bash
curl -X POST "http://localhost:8000/api/v1/horses" \
  -H "Content-Type: application/json" \
  -d '{
    "nickname": "Гром",
    "gender": "жеребец",
    "age": 5,
    "owner_id": 1
  }'
```
Ответ включит `"id": 1`

### Создание состязания
```bash
curl -X POST "http://localhost:8000/api/v1/races" \
  -H "Content-Type: application/json" \
  -d '{
    "date": "2026-03-15",
    "time": "14:30:00",
    "hippodrome": "Московский ипподром",
    "name": "Весенний кубок"
  }'
```
Ответ включит `"id": 1`

### Добавление результата участника
```bash
curl -X POST "http://localhost:8000/api/v1/participants" \
  -H "Content-Type: application/json" \
  -d '{
    "race_id": 1,
    "jockey_id": 1,
    "horse_id": 1,
    "place": 1,
    "time_result": "02:30:00"
  }'
```

### Получение состязания с участниками (ФУНКЦИЯ 1)
```bash
curl "http://localhost:8000/api/v1/races/1"
```

### Получение состязаний жокея (ФУНКЦИЯ 6)
```bash
curl "http://localhost:8000/api/v1/jockeys/1/races"
```

### Получение состязаний лошади (ФУНКЦИЯ 7)
```bash
curl "http://localhost:8000/api/v1/horses/1/races"
```

## 🧪 Запуск тестов

```bash
# Все тесты
pytest tests/ -v

# Конкретный тест
pytest tests/test_race_operations.py::test_create_race_success -v

# С покрытием
pytest tests/ --cov=src
```

## 🐳 Docker

### Создание образа
```bash
cd backend
docker build -t racetracker-backend .
```

### Запуск контейнера
```bash
docker run -p 8000:8000 racetracker-backend
```

## 📊 Основные компоненты

### Framework Layer (src/framework/)
- `schemas.py` - Pydantic модели для API
- `dependencies.py` - Dependency Injection
- `api/v1/` - 5 роутеров для endpoints

### Business Layer (src/business/)
- `operations/` - 5 файлов с операциями (15+ функций)
- `dto/` - 5 DTO классов для безопасности типов
- `exceptions.py` - Бизнес-исключения

### Data Layer (src/data/)
- `models.py` - 6 SQLAlchemy моделей
- `repositories/` - 6 репозиториев (база + 5 специализированных)
- `uow.py` - Unit of Work для управления транзакциями
- `database.py` - Конфигурация подключения к БД

## 🔄 Основной workflow

1. **Frontend отправляет запрос** → Framework layer
2. **Framework валидирует данные** → Business layer
3. **Business применяет правила** → Data layer
4. **Data layer работает с БД** → Repository pattern
5. **Unit of Work коммитит/откатывает** → Transaction management
6. **Ответ возвращается клиенту** с данными или ошибкой

## 📋 Список всех endpoints

### Состязания (Races)
- `POST /api/v1/races` - Создать
- `GET /api/v1/races` - Список (пагинация)
- `GET /api/v1/races/{id}` - Получить с участниками

### Жокеи (Jockeys)
- `POST /api/v1/jockeys` - Создать
- `GET /api/v1/jockeys` - Список
- `GET /api/v1/jockeys/{id}` - Получить
- `GET /api/v1/jockeys/{id}/races` - Состязания

### Лошади (Horses)
- `POST /api/v1/horses` - Создать
- `GET /api/v1/horses` - Список
- `GET /api/v1/horses/{id}` - Получить
- `GET /api/v1/horses/{id}/races` - Состязания

### Владельцы (Owners)
- `POST /api/v1/owners` - Создать
- `GET /api/v1/owners` - Список
- `GET /api/v1/owners/{id}` - Получить

### Участники (Participants)
- `POST /api/v1/participants` - Добавить результат

### Утилиты
- `GET /health` - Health check

## 💡 Важные замечания

1. **Асинхронность**: Все операции асинхронны (async/await)
2. **Валидация**: Все входные данные проверяются Pydantic
3. **Бизнес-правила**:
   - Дата состязания не может быть в прошлом
   - Жокей должен быть старше 16 лет
   - Владелец должен существовать перед созданием лошади
   - Пара жокей-лошадь не может участвовать дважды в одном состязании

4. **База данных**: По умолчанию SQLite в памяти
   - Для PostgreSQL: `DATABASE_URL=postgresql+asyncpg://user:pass@localhost/racetracker`

## 🔗 Полная документация

- `backend/README.md` - Полная документация backend
- `dev/BACKEND_INSTRUCTIONS.md` - Исходные инструкции
- `IMPLEMENTATION_SUMMARY.md` - Отчет о реализации

## ❓ Часто задаваемые вопросы

**Q: Как добавить PostgreSQL?**
A: Установите пакет `pip install psycopg2-binary`, затем установите переменную окружения `DATABASE_URL=postgresql+asyncpg://user:pass@host/db`

**Q: Как запустить на другом порту?**
A: `python -m uvicorn src.main:app --port 8080`

**Q: Как отключить автоперезагрузку?**
A: Удалите флаг `--reload`: `python -m uvicorn src.main:app`

**Q: Как изменить CORS origins?**
A: Отредактируйте `src/main.py` и измените `allow_origins` в настройке middleware

## 🎉 Готово!

Ваш backend полностью работает и готов к интеграции с фронтенд приложением!
