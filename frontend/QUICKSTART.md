# Быстрый старт RaceTracker Frontend

## 🚀 Запуск приложения

### Вариант 1: Python встроенный веб-сервер (Рекомендуется)

```bash
cd frontend
python3 -m http.server 5173
```

Откройте в браузере: **http://localhost:5173**

### Вариант 2: Node.js (если установлен)

```bash
cd frontend
npx http-server -p 5173
```

### Вариант 3: Docker

```bash
# Сборка образа
docker build -t racetracker-frontend .

# Запуск контейнера
docker run -p 80:80 racetracker-frontend

# Откройте http://localhost
```

## ⚙️ Конфигурация API

Отредактируйте файл `.env` в корне `frontend/`:

```env
API_BASE_URL=http://localhost:8000
```

**Важно:** Backend должен быть запущен на `http://localhost:8000`

## 📋 Требования

- **Браузер:** современный браузер с поддержкой ES6+
  - Chrome 51+
  - Firefox 54+
  - Safari 10+
  - Edge 15+

- **Сервер:** Python 3 или Node.js (для локальной разработки)

- **Backend:** Должен быть запущен на `http://localhost:8000`

## 🎯 Основной функционал

### Состязания
- Перейти на главную страницу (`#/`) для просмотра списка
- Нажать "+ Добавить состязание" для создания нового
- Кликнуть на карточку для просмотра деталей и результатов

### Жокеи
- Перейти на страницу "Жокеи" (`#/jockeys`)
- Нажать "+ Добавить жокея" для добавления нового
- Кликнуть на жокея для просмотра профиля и истории

### Лошади
- Перейти на страницу "Лошади" (`#/horses`)
- Нажать "+ Добавить лошадь" для добавления новой
- Кликнуть на лошадь для просмотра профиля и истории

### Добавление результата состязания

Результаты добавляются через форму "Добавить результат состязания":

1. Выберите состязание
2. Выберите жокея
3. Выберите лошадь
4. Укажите место и время (опционально)
5. Нажмите "Добавить результат"

## 🔗 URL маршруты

- `#/` - Список состязаний
- `#/races/:id` - Деталь состязания
- `#/jockeys` - Список жокеев
- `#/jockeys/:id` - Профиль жокея
- `#/horses` - Список лошадей
- `#/horses/:id` - Профиль лошади

## 🐛 Отладка

Откройте консоль браузера (`F12` → Console) для просмотра логов:

```javascript
// Проверить подключение к API
fetch('http://localhost:8000/api/v1/races')
  .then(r => r.json())
  .then(console.log)
```

## 📊 Структура проекта

```
frontend/
├── index.html           # Главная HTML страница
├── src/
│   ├── main.js         # Точка входа
│   ├── app/            # Инициализация приложения
│   ├── pages/          # Страницы приложения
│   ├── features/       # Функциональные модули
│   ├── widgets/        # Компоненты
│   ├── entities/       # API для сущностей
│   ├── shared/         # Общий код
│   └── styles/         # CSS стили
├── .env                # Конфигурация
├── nginx.conf          # Конфигурация для production
└── Dockerfile          # Docker образ
```

## 🚨 Решение проблем

### CORS ошибка
Убедитесь, что backend запущен и CORS включен. Если ошибка персистирует, используйте Nginx proxy (см. `nginx.conf`).

### API не отвечает
1. Проверьте, запущен ли backend на `http://localhost:8000`
2. Проверьте конфигурацию в `.env`
3. Откройте консоль браузера для деталей ошибки

### Страница не загружается
1. Проверьте консоль браузера на ошибки JavaScript
2. Убедитесь, что используется современный браузер
3. Очистите кеш браузера (Ctrl+Shift+Delete)

## 📝 Примечания

- Приложение не требует build процесса
- Все данные загружаются с backend через API
- Локальное кеширование GET запросов для оптимизации
- Навигация реализована через hash routes

## 🔒 Production развертывание

```bash
# Использовать Docker
docker build -t racetracker-frontend .
docker run -d -p 80:80 racetracker-frontend

# или с docker-compose
docker-compose up frontend
```

## 📞 Помощь

Для информации о архитектуре и техническим деталям см. `README.md`.
