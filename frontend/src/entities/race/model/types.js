/**
 * Race Entity Types
 */

// Types are implicit in JavaScript, but we document them here
// Race: { id, date, time, hippodrome, name? }
// ParticipantResult: { jockey_name, horse_name, place, time_result? }
// RaceWithParticipants: { race, participants }
// CreateRaceDTO: { date, time, hippodrome, name? }
