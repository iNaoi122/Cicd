# 🔍 RaceTracker Frontend - Инструкция по отладке

**Если фронтенд не отображается правильно, следуйте этим шагам:**

## 1. Базовая проверка

Откройте в браузере: **http://localhost**

Вы должны увидеть одно из двух:
- ⏳ Сообщение "Загрузка приложения RaceTracker..."
- ❌ Сообщение об ошибке с описанием проблемы

## 2. Откройте Консоль Браузера

В браузере нажмите **F12** или **Ctrl+Shift+I** (Windows/Linux) / **Cmd+Option+I** (Mac)

Перейдите на вкладку **Console** (Консоль)

## 3. Посмотрите на Логи

В консоли вы должны увидеть логи в порядке:
```
📝 index.html загружен
🚀 Начало загрузки модулей
📥 main.js: Начало загрузки...
✅ DOM готов, инициализируем приложение...
📦 Импортируем app.js...
✅ app.js загружен
🚀 Запускаем initApp()...
✅ Приложение успешно инициализировано!
```

## 4. Если вы видите ошибку

Ошибка будет отображена:
- В консоли (красным текстом)
- На странице (красное поле с текстом ошибки)

**Типичные ошибки:**

### 404 Not Found
```
Failed to fetch module: ./src/app/app.js
```
**Решение:** Проверьте что файлы находятся в правильных папках

### CORS Error
```
Cross-Origin Request Blocked
```
**Решение:** Это может быть проблема с API, но она не должна прерывать загрузку UI

### SyntaxError
```
Unexpected token...
```
**Решение:** В коде JavaScript синтаксическая ошибка. Проверьте консоль на точное местоположение

## 5. Проверка сетевых запросов

На вкладке **Network** (Сеть) проверьте:

- **index.html** - должен быть **200 OK**
- **src/main.js** - должен быть **200 OK**
- **src/app/styles/index.css** - должен быть **200 OK**
- **src/app/app.js** - должен быть **200 OK**

Если любой из этих файлов имеет статус **404**, файл не загружается.

## 6. Проверка содержимого контейнера

```bash
# Проверить есть ли файлы в контейнере
docker exec racetracker-frontend ls -la /usr/share/nginx/html/src/app/

# Проверить содержимое конкретного файла
docker exec racetracker-frontend cat /usr/share/nginx/html/src/app/app.js | head -20
```

## 7. Перезагрузка контейнера

Если ничего не помогает:

```bash
# Перезагрузить только frontend
docker restart racetracker-frontend

# Или пересоздать оба контейнера
docker-compose down
docker-compose up -d
```

## 8. Проверка Nginx конфигурации

```bash
# Убедиться что Nginx использует правильную конфигурацию
docker exec racetracker-frontend cat /etc/nginx/nginx.conf | grep -A 10 "location /"
```

## 9. Просмотр логов Nginx

```bash
# Логи frontend контейнера
docker logs racetracker-frontend

# Логи backend контейнера
docker logs racetracker-backend
```

## Ожидаемый результат

После успешной загрузки вы должны увидеть:
- 🎯 Заголовок "RaceTracker" в левом углу
- 📋 Навигационное меню сверху (Состязания, Жокеи, Лошади)
- 🏇 Список состязаний на главной странице

## Если приложение загружается, но выглядит странно

1. **Проверьте CSS:**
   - Откройте вкладку **Elements/Inspector**
   - Проверьте стили элемента (F12 → правый клик на элемент → Inspect)
   - Стили должны применяться

2. **Проверьте JavaScript ошибки:**
   - В консоли найдите красные сообщения об ошибках
   - Проверьте что все импорты работают

3. **Очистите кеш браузера:**
   - Нажмите **Ctrl+Shift+Delete** (или Cmd+Shift+Delete на Mac)
   - Выберите "All time" (все время)
   - Нажмите "Clear data"

## Контрольный список

- ✅ Frontend контейнер работает (`docker ps | grep frontend`)
- ✅ Backend контейнер работает (`docker ps | grep backend`)
- ✅ Frontend доступен на http://localhost
- ✅ Backend доступен на http://localhost:8000/health
- ✅ API работает на http://localhost:8000/api/v1/races
- ✅ CSS загружается без ошибок 404
- ✅ JavaScript файлы загружаются без ошибок 404
- ✅ Консоль браузера не показывает красных ошибок

## Быстрая диагностика

```bash
# Все в одной команде
echo "=== Frontend ===" && \
curl -s http://localhost/ > /dev/null && echo "✅ Frontend доступен" || echo "❌ Frontend недоступен" && \
echo "" && \
echo "=== Backend ===" && \
curl -s http://localhost:8000/health | grep -q "healthy" && echo "✅ Backend здоров" || echo "❌ Backend не здоров" && \
echo "" && \
echo "=== Контейнеры ===" && \
docker ps | grep racetracker
```

## Дополнительная помощь

- Посмотрите логи: `docker-compose logs -f`
- Проверьте хост-файлы: `ls -la /home/sergey/University/cicd/frontend/src/`
- Проверьте файлы в контейнере: `docker exec racetracker-frontend find /usr/share/nginx/html/src -type f`

---

Если вы видите все логи загрузки успешно, но приложение выглядит пусто, это может быть:
1. Проблема с отображением компонентов
2. API не возвращает данные (используется пустая БД)
3. CSS не применяется правильно

В этом случае проверьте консоль браузера на ошибки JavaScript и свяжитесь с разработчиком.
